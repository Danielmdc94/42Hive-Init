# Find the command that allows you to connect via ssh on the VM. (In parallel with the graphic session)

ssh -p 3022 dpalacio@127.0.0.1

# I configured the port 3022 of the virtual machine to connect to the port 22 (default ssh) of the computer
# The IP shown is a reference to a machine's own IP
