# Path of the file that contains the IP address of my DNS server

/etc/resolv.conf

# Usually this file contais a set of configuration options for the resolver
# In this case it only stores de address and domain of the DNS server
