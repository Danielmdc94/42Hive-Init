# Find the provider of who.int

# First we find with the command "nslookup" the IP of who.int (104.17.113.188)
# Then, we use the command "whois" on that IP
# On the resulting output we can find that the provider of such domain is Cloudflare
